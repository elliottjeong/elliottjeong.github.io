# Eunah Jeong — Portfolio

A dependency-free static portfolio built with HTML and CSS and designed with a museum-gallery visual direction.

Open `index.html` in a browser to view the site locally.

To preview it over HTTP instead, run:

```sh
python3 -m http.server 8000 --bind 127.0.0.1
```

The site is intended for deployment with GitHub Pages.
